import 'package:flutter/material.dart';

class WorkoutsLayout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Workouts under construction'),
    );
  }
}
